import { SearchPage } from "@/components/SearchPage";

export const metadata = {
  title: "Search Korea | Daedongyeojido",
  description: "Search restaurants, attractions, and experiences across Korea.",
};

export default function SearchRoute() {
  return <SearchPage />;
}
